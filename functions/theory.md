cannot create custom functions

String function - upper(), lower(), trim(), replace(), count()

File function - 

Numeric function - 

Collection function - length([1,2,3]), concat(list1, list2), merge({a=1},{b=2})

Type conversion - toset(["a", "b", "c"]), tonumber("7")

Date and time function - timestamp()

Lookup function - 

Validation function - 